import { h, render, Component } from 'preact';

export default class BottomBar extends Component {

    render() {
        return (
            <div>
                <p>Bottom</p>
            </div>
        );
    }
}