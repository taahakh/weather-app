import { h, render, Component } from 'preact';

export default class TopBar extends Component {

    render() {
        return (
            <div>
                <p>ELLO PLS WORK</p>
            </div>
        );
    }
}